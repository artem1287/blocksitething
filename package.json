{
  "name": "taper-monorepo",
  "private": true,
  "version": "0.0.1",
  "workspaces": [
    "packages/*",
    "extension",
    "backend",
    "website"
  ],
  "scripts": {
    "test": "npm run test --workspace=@taper/engine",
    "test:watch": "npm run test:watch --workspace=@taper/engine"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
